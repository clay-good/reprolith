REPRODUCTION CERTIFICATE — 1-D diffusion of a Gaussian (D=0.5)
Engine pin: reprolith-fd 0.0.1 / explicit-forward-euler-finite-difference (rev d41d7decbbd2)

OVERALL: partially-reproduced
  claims by verdict: reproduced=1, partial=0, failed=0, not-evaluable=0
  assumption-qualified claims: diffusion_Dhalf-profile

CLAIMS
  [diffusion_Dhalf-profile] diffused concentration profile: reproduced [assumption-qualified] (source closed-form via curve-normalized-distance, tol=reproduced<=0.1, partial<=0.25 (class-default))
      measured: normalized distance 0.0000, worst point 0.0001 of reference scale (pass budget 0.2500)
      protocol: 1-D finite difference: D=0.5, dx=0.2, dt=0.016000000000000004, 1200 steps, zero-flux (Neumann) boundaries (that wall costs this claim 1.741e-06: the judged distance moves from 1.998e-05 to 2.172e-05 under dirichlet, against a pass threshold of 1.000e-01)

ASSUMPTIONS (supplied by Reprolith, not the paper)
  [spatial-boundary-diffusion_Dhalf-profile] the profile judged here was evolved under zero-flux (Neumann) boundaries; Reprolith did not check what boundary the source specifies -> zero-flux (Neumann) boundaries [load-bearing] [this engine's limit — no expert decision closes it]
      basis: a claim carries no field naming a boundary, so this run's wall is this engine's choice rather than anything a paper could state. What the choice costs is no longer a caveat: each claim's protocol line reports how far re-running the same discretization under the alternatives this solver implements moves the judged distance, against the threshold it is judged at. An unbounded domain is not among those alternatives and is not measured (attributed to reprolith)

WHAT WAS MISSING
  [diffusion_Dhalf-profile] diffused concentration profile: reproduced only under an assumption Reprolith supplied, not a clean pass
  the profile judged here was evolved under zero-flux (Neumann) boundaries; Reprolith did not check what boundary the source specifies — Reprolith assumed zero-flux (Neumann) boundaries (a claim carries no field naming a boundary, so this run's wall is this engine's choice rather than anything a paper could state. What the choice costs is no longer a caveat: each claim's protocol line reports how far re-running the same discretization under the alternatives this solver implements moves the judged distance, against the threshold it is judged at. An unbounded domain is not among those alternatives and is not measured)

SCOPE
  This certificate attests only to the computational reproducibility of the paper's own results. It makes no claim about biological correctness, model appropriateness, or fitness for any clinical or therapeutic decision.