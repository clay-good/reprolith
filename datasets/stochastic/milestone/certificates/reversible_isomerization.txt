REPRODUCTION CERTIFICATE — Reversible isomerization A<->B (N=50, kf=3, kr=1) — binomial equilibrium mean
Engine pin: reprolith-ssa 0.0.1 / gillespie-direct-method (rev b87a82bb6490)

OVERALL: partially-reproduced
  claims by verdict: reproduced=1, partial=0, failed=0, not-evaluable=0
  assumption-qualified claims: reversible_isomerization-mean

CLAIMS
  [reversible_isomerization-mean] mean copy number: reproduced [assumption-qualified] (source closed-form via scalar-relative-error, tol=reproduced<=0.05, partial<=0.15 (class-default))
      measured: relative error 0.0063
      protocol: SSA ensemble: 400 trajectories to t=30, seed 1234, read=species[1] (sampling noise: the mean's standard error is 0.42% of the reported value, against a 5% pass threshold)

ASSUMPTIONS (supplied by Reprolith, not the paper)
  [ssa-sampling-reversible_isomerization-mean] the mean judged here is the average of an ensemble Reprolith sampled, not a number the paper's own run produced -> SSA ensemble: 400 trajectories to t=30, seed 1234, read=species[1] [load-bearing] [this engine's limit — no expert decision closes it]
      basis: a finite ensemble's estimate differs from the model's true value by sampling noise of a size the trajectory count sets, so the verdict moves with the count and the seed; both are pinned here to make it byte-reproducible. How far it moves is measured rather than left as a caveat: this claim's protocol line reports that estimate's standard error as a share of the reported value, against the threshold it is judged at (attributed to reprolith)

WHAT WAS MISSING
  [reversible_isomerization-mean] mean copy number: reproduced only under an assumption Reprolith supplied, not a clean pass
  the mean judged here is the average of an ensemble Reprolith sampled, not a number the paper's own run produced — Reprolith assumed SSA ensemble: 400 trajectories to t=30, seed 1234, read=species[1] (a finite ensemble's estimate differs from the model's true value by sampling noise of a size the trajectory count sets, so the verdict moves with the count and the seed; both are pinned here to make it byte-reproducible. How far it moves is measured rather than left as a caveat: this claim's protocol line reports that estimate's standard error as a share of the reported value, against the threshold it is judged at)

SCOPE
  This certificate attests only to the computational reproducibility of the paper's own results. It makes no claim about biological correctness, model appropriateness, or fitness for any clinical or therapeutic decision.